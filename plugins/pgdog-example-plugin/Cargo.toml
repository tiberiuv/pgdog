[package]
name = "pgdog-example-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
pgdog-plugin = { version = "0.1.7", path = "../../pgdog-plugin" }
once_cell = "1"
parking_lot = "0.12"
thiserror = "2"
